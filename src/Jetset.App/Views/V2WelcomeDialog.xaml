<Window x:Class="Jetset.App.Views.V2WelcomeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Welcome"
        Width="460"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">
  <StackPanel Margin="16">
    <TextBlock Text="{Binding Title}"
               FontSize="20"
               FontWeight="SemiBold"
               FontFamily="{DynamicResource DisplayFont}"
               Margin="0,0,0,12" />
    <TextBlock Text="{Binding IntroText}"
               TextWrapping="Wrap"
               Margin="0,0,0,16" />

    <TextBlock Text="What's new"
               FontWeight="SemiBold"
               Margin="0,0,0,8" />
    <ItemsControl ItemsSource="{Binding Highlights}" Margin="0,0,0,16">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <TextBlock Text="{Binding StringFormat=• {0}}"
                     TextWrapping="Wrap"
                     Margin="0,0,0,6" />
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>

    <TextBlock Text="Keyboard shortcuts"
               FontWeight="SemiBold"
               Margin="0,0,0,8" />
    <ItemsControl ItemsSource="{Binding Shortcuts}" Margin="0,0,0,16">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Grid Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="100" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="{Binding Shortcut}"
                       FontFamily="{DynamicResource MonospaceFont}"
                       FontWeight="SemiBold" />
            <TextBlock Grid.Column="1"
                       Text="{Binding Action}"
                       TextWrapping="Wrap" />
          </Grid>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>

    <Button Content="Get started"
            Click="GetStarted_Click"
            Style="{DynamicResource PrimaryButton}"
            HorizontalAlignment="Left" />
  </StackPanel>
</Window>
