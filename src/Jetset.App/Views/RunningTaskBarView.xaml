<UserControl x:Class="Jetset.App.Views.RunningTaskBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:Jetset.App.Helpers">
  <UserControl.Resources>
    <helpers:BoolToVisibilityConverter x:Key="BoolToVis" />
  </UserControl.Resources>

  <Border Padding="12,8"
          Background="{DynamicResource PanelBackgroundBrush}"
          BorderBrush="{DynamicResource BorderBrush}"
          BorderThickness="0,1,0,0">
    <Grid>
      <TextBlock Text="No task running — start from the tree or press Ctrl+N"
                 VerticalAlignment="Center"
                 Foreground="{DynamicResource TextSecondaryBrush}"
                 Visibility="{Binding HasRunningTask, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}" />

      <DockPanel Visibility="{Binding HasRunningTask, Converter={StaticResource BoolToVis}}">
        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
          <Button Content="Done"
                  Command="{Binding MarkDoneCommand}"
                  Style="{DynamicResource SecondaryButton}"
                  Margin="0,0,6,0" />
          <Button Content="Waiting"
                  Command="{Binding MarkWaitingCommand}"
                  Style="{DynamicResource SecondaryButton}"
                  Margin="0,0,6,0" />
          <Button Content="Pause"
                  Command="{Binding PauseCommand}"
                  Style="{DynamicResource SecondaryButton}"
                  Margin="0,0,6,0"
                  Visibility="{Binding IsPaused, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}" />
          <Button Content="Resume"
                  Command="{Binding ResumeCommand}"
                  Style="{DynamicResource PrimaryButton}"
                  Visibility="{Binding IsPaused, Converter={StaticResource BoolToVis}}" />
        </StackPanel>

        <StackPanel VerticalAlignment="Center">
          <TextBlock Text="{Binding TaskTitle}"
                     FontWeight="SemiBold"
                     TextTrimming="CharacterEllipsis" />
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding StatusText}"
                       FontSize="11"
                       Foreground="{DynamicResource AccentBrush}"
                       Margin="0,0,12,0" />
            <TextBlock Text="{Binding TimerDisplay}"
                       FontSize="11">
              <TextBlock.Style>
                <Style TargetType="TextBlock">
                  <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding IsOvertime}" Value="True">
                      <Setter Property="Foreground" Value="{DynamicResource DangerBrush}" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </TextBlock.Style>
            </TextBlock>
          </StackPanel>
        </StackPanel>
      </DockPanel>
    </Grid>
  </Border>
</UserControl>
